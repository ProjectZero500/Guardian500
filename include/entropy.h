#ifndef ENTROPY_H
#define ENTROPY_H

#include <iostream>
#include <fstream>
#include <vector>
#include <cmath>
#include <unordered_map>
#include "resource.h"

double calculateFileEntropy(const Resource& resource);

#endif // ENTROPY_H